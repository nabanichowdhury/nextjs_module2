import { NextResponse } from "next/server"

export const GET=()=>{
    return NextResponse.json({message:"Hello from the server"}) ;
}
export const POST=()=>{
    // return NextResponse.json({message:"Hello from the server"}) ;
}
export const PUT=()=>{
    // return NextResponse.json({message:"Hello from the server"}) ;
}
export const DELETE=()=>{
    // return NextResponse.json({message:"Hello from the server"}) ;
}